# Pinned to the versions the live bot runs on (Python 3.9 venv on the Mac).
# scikit-learn is pinned exactly: model pickles are tied to the version that
# wrote them (a newer version loads them with InconsistentVersionWarning;
# an older one may not load them at all).
requests==2.32.5
pandas==2.3.3
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
cryptography==48.0.0
flask==3.1.3
matplotlib>=3.7.0
pytest==8.4.2
